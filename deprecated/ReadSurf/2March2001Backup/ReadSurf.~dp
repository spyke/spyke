program ReadSurf;

uses
  Forms,
  ReadSurfMain in 'ReadSurfMain.pas' {ReadSurfForm},
  WaveFormUnit in '..\Public\WaveFormUnit.pas' {WaveFormWin},
  SurfPublicTypes in '..\Public\SurfPublicTypes.pas';

{$R *.RES}

begin
  Application.Initialize;
  Application.CreateForm(TReadSurfForm, ReadSurfForm);
  //Application.CreateForm(TWaveFormWin, WaveFormWin);
  Application.Run;
end.
