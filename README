For now, has these specific version dependencies:

Cython 0.9.6.14 (0.9.8 doesn't like from __future__ import division)
wxPython 2.8.7.1 (2.8.8.0 has keyboard entry issue for spinctrls or comboboxes or something)

To install:

>>> python setup.py install

To develop:

>>> python setup.py build_ext --compiler=mingw32 --inplace
